# コメント
 # 1
	ゲーム性は高く見えるがプログラマとしてどこまで出来るかという軸ではややボリュームにかける。

 # 2
	void CEnemy::SpawnManager(void)
		else if (CTime::GetTime() / 1000 >= 40)
		{// 60秒
	固定値で調整していてコードが長くなればなるほどより調整しづらいので外部ファイルなどに置く。

 # 3
	// 湧く数のランダム
	int nRand = rand() % 5 + 3;
	ぱっと見,+3が何を表してるのか不明なので意味のある数字はマクロやconstに置き換える。
